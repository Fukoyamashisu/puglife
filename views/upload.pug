extends layout

block form
    form(method='POST', enctype='multipart/form-data', action='monupload')
        form
            .form-inline
                input#controlFile.form-inline(type='file' name="files" multiple)
                button.btn.btn-dark(type='submit') Upload

